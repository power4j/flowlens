# 应用跟随当前稳定 Rust 版本

FlowLens 是面向终端用户交付的应用程序，不以维护较低版本 Rust 的库兼容性为目标。项目 Rust 基线升级到当前稳定版 `1.96.0`，并在 `Cargo.toml`、日常 CI、Release 构建和开发文档中统一记录。

Release 仍固定使用 `1.96.0`，以保证同一版本的制品可以复现；后续稳定版发布后，按依赖生态需要主动升级基线并同步更新这些位置。该变更只影响构建工具链，不改变 Linux 的 glibc `2.28` 和 libpcap 运行时要求。
